import { createFileRoute } from "@tanstack/react-router";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { profileQuery, settingsQuery } from "@/lib/queries";
import { supabase } from "@/integrations/supabase/client";
export const Route = createFileRoute("/_authenticated/app/configuracoes")({ component: SettingsPage });
function SettingsPage(){const p=useQuery(profileQuery),s=useQuery(settingsQuery),client=useQueryClient();const [name,setName]=useState("");const [whatsapp,setWhatsapp]=useState("");const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);const save=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setStatus("");const {data:{user}}=await supabase.auth.getUser();if(!user){setStatus("Sessão expirada.");setBusy(false);return}const {error}=await supabase.from("profiles").update({full_name:name,whatsapp}).eq("id",user.id);setStatus(error?error.message:"Perfil salvo com sucesso.");setBusy(false);client.invalidateQueries({queryKey:["profile"]});};if(p.isLoading||s.isLoading)return <main className="mx-auto max-w-2xl px-4 py-8">Carregando...</main>;return <main className="mx-auto max-w-2xl px-4 py-8 sm:px-6"><h1 className="font-display text-3xl font-bold">Configurações</h1><form onSubmit={save} className="mt-6 space-y-5 rounded-md border border-border bg-card p-5"><label className="block text-sm">Nome completo<input className="mt-2 w-full rounded-md border border-input bg-background p-2" value={name||p.data?.full_name||""} onChange={e=>setName(e.target.value)}/></label><label className="block text-sm">WhatsApp<input className="mt-2 w-full rounded-md border border-input bg-background p-2" value={whatsapp||p.data?.whatsapp||""} onChange={e=>setWhatsapp(e.target.value)}/></label><button disabled={busy} className="rounded-md bg-primary px-4 py-2 font-medium text-primary-foreground">{busy?"Salvando...":"Salvar perfil"}</button>{status&&<p className="text-sm text-muted-foreground">{status}</p>}</form><p className="mt-5 text-xs text-muted-foreground">Preferências carregadas: {Object.keys(s.data??{}).length} disponíveis.</p></main>}
